const Reward = require('./reward.png');
const ClaimReward = require('./claim-reward.png');
const RewardClaimed = require('./reward-claimed.png');
const EmptyList = require('./empty-list.svg');

export const images = {
  rewardDefault: Reward,
  claimReward: ClaimReward,
  rewardClaimed: RewardClaimed,
  emptyList: EmptyList,
};
